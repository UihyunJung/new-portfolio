# DESIGN.md

이 사이트의 디자인 시스템 — **결정과 제약**을 적는다.

값은 여기 적지 않는다. 색·크기·간격의 실제 값은 토큰 파일 하나뿐이고,
문서가 그걸 베끼는 순간 둘이 어긋나기 시작한다. 이 문서는 "왜 그렇게
정했는가"와 "무엇을 하면 안 되는가"를 담고, 값은 아래 표를 따라간다.

| 무엇           | 어디                                     |
| -------------- | ---------------------------------------- |
| 색             | `app/assets/styles/@tokens/_colors.scss` |
| 타이포         | `@tokens/_typography.scss`               |
| 간격·기하      | `@tokens/_spacing.scss`                  |
| 모션           | `@tokens/_motion.scss`                   |
| 브레이크포인트 | `@tokens/_breakpoints.scss`              |
| 믹스인         | `@utilities/_mixins.scss`                |
| 리셋           | `@global/_reset.scss`                    |

---

## 정체성

**Hallmark · Cobalt** — 모던 미니멀, Workbench 구조. 계기판이나 스펙 시트처럼
읽히는 것이 목표다. 장식이 아니라 **정보의 밀도와 자로 그은 선**이 인상을
만든다.

---

## 색

- **OKLCH 전용.** 다른 색 공간을 섞지 않는다. 명도를 바꿔도 채도가 따라
  무너지지 않아 라이트/다크 한 쌍을 만들기 쉽다.
- **액센트는 하나, 색상 256에 고정.** 어느 화면에서도 5% 미만으로 쓴다.
  액센트가 흔해지면 그것은 더 이상 신호가 아니다.
- **순수 흑백을 쓰지 않는다.** 종이는 `#fff`가 아니고 잉크는 `#000`이 아니다.
  둘 다 차가운 쪽으로 살짝 기울어 있다.
- **다크는 앵커 색상을 유지한 채 명도와 채도만 옮긴다.** 색상을 돌리면 두
  테마가 다른 브랜드처럼 보인다.
- **고도(elevation)를 표현하는 그림자는 하나뿐이다** — `--shadow-sheet`.
  모바일 시트가 살아 있는 콘텐츠 위를 덮을 때만 쓴다. 면을 띄우려고
  그림자를 추가하지 않는다.
- 예외가 하나 있다. 히어로의 주요 CTA는 hover에서 액센트 14%짜리 헤일로
  링(`box-shadow: 0 0 0 8px`)을 쓴다. 이건 고도가 아니라 어포던스이고,
  페이지에서 자석 인력이 붙는 유일한 버튼이라 반응이 필요했다. 다른 곳에
  복제하지 말 것.

### 대비 (실측)

캔버스로 실제 sRGB를 뽑아 측정한 값이다. 새 조합을 만들면 다시 잰다.

| 조합                    | 라이트  | 다크    |
| ----------------------- | ------- | ------- |
| `ink` / `paper`         | 17.03:1 | 15.69:1 |
| `ink-2` / `paper`       | 12.91:1 | 11.30:1 |
| `muted` / `paper`       | 7.38:1  | 5.23:1  |
| `accent-text` / `paper` | 9.64:1  | 5.78:1  |
| `accent-ink` / `accent` | 7.74:1  | 4.98:1  |
| `rule-strong` / `paper` | 4.24:1  | 3.47:1  |

**`rule-strong`은 글자색으로 쓰지 않는다.** 이름 그대로 테두리용이고,
비텍스트 대비 기준(3:1)은 넘지만 본문 기준(4.5:1)에는 못 미친다. 아이콘만
있는 컨트롤의 유일한 경계가 그 용도다.

---

## 타이포그래피

**2+1 조합** — Space Grotesk(디스플레이) + Wanted Sans(본문·한글) +
JetBrains Mono(라벨). 라틴 디스플레이는 Space Grotesk가, 한글은 같은 스택의
Wanted Sans가 받으므로 한영이 섞인 제목도 하나의 목소리로 유지된다.

**mono 라벨은 라틴 토큰 전용이다.** `label` 믹스인은 대문자에 자간 0.08em을
주는데, 한글에는 두 가지가 어긋난다 — 자간이 글자를 벌리고(하이라이트 →
하 이 라 이 트), JetBrains Mono에 한글 글리프가 없어 모노스페이스 대체
폰트가 한 글자를 한 em으로 세운다.

- 필드 이름은 **영문 대문자 토큰**으로 쓴다 — `ROLE`, `STACK`, `TYPE`,
  `HIGHLIGHTS`. 두 언어 모두 같다.
- 라벨 크기의 **한글 문장**은 `label-prose`를 쓴다. 본문 서체, 자간 없음.

**항목명과 내용은 네 가지가 달라야 한다** — 서체·크기·굵기·명도. 크기가 1px
차이 나는 두 가지는 계층이 아니다. 실제로 그렇게 만들었다가 구분이 안 된
적이 있다.

---

## 기하

**구조는 선이 만든다.** 카드도, 테두리 그라데이션도, 떠오르는 효과도 쓰지
않는다. 면을 나눌 때는 헤어라인과 세로 리듬으로 나눈다.

- **선 두께는 토큰 둘뿐이다** — `--rule-hair`(1px), `--rule-solid`(2px).
  하드코딩된 두께는 저장소에 없다.
- **반경은 `--radius-sm`(6px)과 `--radius-md`(10px)** 이 기본이고, 예외가
  셋 있다. 늘리지 말 것:
  - `2px` — 인라인 텍스트 링크의 포커스 링 모서리에만
  - `4px` — 6px 그룹 안에 중첩된 컨트롤(테마 토글의 썸과 옵션).
    안쪽 반경 = 바깥 반경 − 패딩
  - `50%` — 경력 타임라인의 노드 점 하나
- **간격의 리듬은 4pt 토큰을 따른다.** 다만 컨트롤 고유의 치수(44px 터치
  타깃, 26px 토글 옵션, 148px 썸네일, 11rem 메타 레일)는 리터럴로 쓴다.
  그건 리듬이 아니라 그 물체의 크기다.

---

## 모션

**전부 CSS다.** JS 애니메이션 라이브러리를 쓰지 않는다. motion/react는 한때
있었지만 모든 모션이 CSS로 옮겨간 뒤에도 초기 JS에 65KB를 얹고 아무것도
애니메이션하지 않아 제거했다.

### 절대 규칙 두 가지

1. **숨김 상태를 기본 규칙에 쓰지 않는다.** 기본 규칙이 정지 상태이고
   키프레임이 _숨김에서_ 출발한다. 스크롤 타임라인 미지원 브라우저와
   reduced-motion 사용자는 완성된 레이아웃을 본다.
2. **JS로 콘텐츠를 숨기지 않는다.** 라이브러리의 `initial` 상태는 서버 HTML에
   기록되므로, 페이드인 진입은 하이드레이션 전까지(실패하면 영구히) 콘텐츠를
   안 보이게 만든다. 히어로의 CTA가 실제로 그렇게 사라졌었다.

같은 이유로 **스크롤이 넘기는 챕터는 `visibility`가 아니라 `opacity` +
`pointer-events`로 숨긴다.** `visibility`는 접근성 트리에서도 빠지는데,
스크린 리더는 스크롤로 진행률을 바꾸지 않으니 숨은 챕터를 영영 못 만난다.
키보드 포커스가 숨은 컨트롤에 닿으면 `:focus-within`이 그 챕터를 연다.

### 층

- **스크롤 연동** — `animation-timeline: view()` / `scroll(root)`.
  `@utilities/_mixins.scss`의 스크롤 구동 믹스인을 쓴다. 히어로의 핀
  스테이지(500svh, 네 장면, 프로필 행)도 `--pin` 뷰 타임라인에
  묶인 CSS다. 스크롤 모션을 JS로 구동하는 곳은 **`HeroCanvas` 하나뿐**이다 —
  CSS 타임라인이 canvas에 닿지 않아서다. 스크롤 진행률에 시간(느린 위상
  흐름)과 커서(가까운 선의 휨)를 더해 그리므로 rAF 루프가 있지만, **히어로가
  화면에 있고 탭이 보일 때만** 돈다. 핀 여부는 CSS 가드가 정한 계산값을
  읽기만 하고, 콘텐츠를 숨기지 않으며, reduced-motion에서는 정지한 한 프레임만
  그린다. 다른 곳에 복제하지 말 것.
- **로드 진입** — 히어로만. `SplitText`의 글자 단위 상승과 블록 큐.
- **포인터** — 자석 CTA(`useMagnetic`)와 커서 스포트라이트(`usePointerField`).
  둘 다 CSS 커스텀 프로퍼티만 쓰고 리렌더를 일으키지 않으며, coarse
  포인터·reduced-motion에서는 아예 붙지 않는다.

### 이징

**이징은 토큰 네 종류가 전부다.** 컴포넌트 SCSS 어디에도 하드코딩된
`cubic-bezier`가 없다. 브라우저 기본값 `ease`는 금지 — 다듬지 않은 모션의
모양이다.

**지속시간 토큰은 상호작용용이다.** `--dur-micro` `short` `long` `travel`.
지연 값과 히어로의 앰비언트 루프(스캔 11s, 티커 64s, 큐 2.2s 같은 것)는
리터럴로 쓴다. 그건 상호작용 응답이 아니라 그 장면의 호흡이라 눈금이 다르다.

**유일한 예외는 `linear`**, 그것도 **표면을 가로질러 지나가는 띠**에만
쓴다(스킬 행 hover sweep). 네 이징은 전부 *안착*을 만들려고 앞뒤로 쏠려
있어서, 도착하지 않고 지나가기만 하는 움직임에 쓰면 번쩍임이 된다.

**스프링(`--ease-spring`)은 도착하는 물체 전용이다** — 시트, 레일을 건너뛰는
틱, 확인 표시. 색이나 투명도에 쓰면 오버슈트가 오류로 보인다.

---

## 컴포넌트 어휘

- **스펙 표** — 항목명 위에 내용. 필드마다 헤어라인. 레일 폭은 11rem이고,
  경력 섹션과 프로젝트 섹션이 같은 치수를 공유한다.
- **칩** — 나열해야 하는 짧은 토큰(기술 스택)에 쓴다. 긴 mono 문장으로 두면
  읽어야 하지만 칩으로 두면 눈이 센다.
- **펼침** — `<details>`를 쓴다. JS가 들지 않고, 내용이 서버 HTML에 남아
  find-in-page·인쇄·하이드레이션 실패에서도 살아 있으며, 키보드와 스크린
  리더 동작이 브라우저 몫이다.
- **오버레이** — 네이티브 `<dialog>`. 포커스 가둠, Esc, 배경 비활성화,
  최상위 레이어가 전부 브라우저에서 온다.
- **눈금** — 슬라이더나 캐러셀의 위치 표시는 점이 아니라 짧은 막대다. 이
  시스템은 선으로 그린다.

---

## 하지 않는 것

- 카드, 테두리 그라데이션, 떠오르는 그림자
- 그라데이션 배경, 글자에 넣는 그라데이션 채움
- 크롬 다시 그리기(가짜 윈도우 바, 가짜 상태 표시줄, 가짜 키보드)
- 콘텐츠가 아닌 숫자·아이콘·통계
- 브랜드가 쓰지 않는 이모지
- 데이터가 아닌 것을 데이터처럼 보이게 하기

---

## 시안

`.design/` 아래에 Claude Design 캔버스 소스가 있다. `.dc.html` 하나가
아트보드 하나이고, `canvas.json`이 배치를 담는다. 생성물(`*.html`)은
gitignore 대상이며 소스에서 다시 만든다.

- `.design/motion/` — Kinetic Cobalt 모션 시안
- `.design/projects/` — 프로젝트 섹션 재구성 시안
